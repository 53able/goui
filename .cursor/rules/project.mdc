---
description: UI Sample Admin プロジェクトの開発ルール・コーディング規約
globs: ["**/*.ts", "**/*.tsx", "**/*.js", "**/*.jsx"]
alwaysApply: true
---

# UI Sample Admin - Project Rules

> pnpm workspaces + Turborepo によるモノレポ構成の管理画面ボイラープレート

---

## 🎯 Project Identity

| 項目 | 内容 |
|------|------|
| **構成** | pnpm workspaces + Turborepo モノレポ |
| **目的** | エンタープライズ級管理画面の高速構築 |
| **アーキテクチャ** | React 19 + shadcn/ui + Hono |

> 📝 以下のドキュメントでは `@myorg/*` をパッケージ名のプレースホルダーとして使用。実際のプロジェクトに合わせて変更してください。

---

## 📦 Monorepo Structure

```
project-root/
├── apps/
│   ├── web/                    # @myorg/web - メインWebアプリ
│   │   ├── src/                # フロントエンドソース
│   │   ├── server/             # Hono APIサーバー
│   │   ├── api/                # Vercel Edge Functions
│   │   ├── package.json
│   │   ├── vite.config.ts
│   │   └── vercel.json
│   │
│   └── admin/                  # @myorg/admin - 管理画面（例）
│       ├── src/
│       ├── package.json
│       └── vite.config.ts
│
├── packages/
│   ├── shared/                 # @myorg/shared - 共有スキーマ・型定義
│   │   ├── src/
│   │   │   ├── schemas.ts      # Zodスキーマ（型の単一ソース）
│   │   │   └── index.ts
│   │   └── package.json
│   │
│   ├── ui/                     # @myorg/ui - 共有UIコンポーネント
│   │   ├── src/
│   │   │   ├── AlertDialog.tsx
│   │   │   ├── Button.tsx
│   │   │   ├── Checkbox.tsx
│   │   │   ├── Input.tsx
│   │   │   ├── Toaster.tsx     # sonner ベースのトースト
│   │   │   ├── utils.ts        # cn() 関数
│   │   │   └── index.ts
│   │   └── package.json
│   │
│   └── ai/                     # @myorg/ai - AI SDK 共有パッケージ
│       ├── src/
│       │   ├── index.ts        # AI SDK コア機能エクスポート
│       │   ├── react.ts        # React用情報
│       │   └── agents/         # 共有エージェント定義
│       │       └── chatAgent.ts
│       └── package.json
│
├── pnpm-workspace.yaml         # ワークスペース定義
├── turbo.json                  # Turborepo タスク定義
├── package.json                # ルート（共通devDeps + turboスクリプト）
├── biome.json                  # 共通Lint/Format設定
└── tsconfig.json               # ルートTS設定（references）
```

---

## 🛠️ Tech Stack

### Monorepo Tools

| ツール | バージョン | 用途 |
|--------|-----------|------|
| **pnpm** | ^9.1.1 | パッケージマネージャー（workspaces） |
| **Turborepo** | ^2.5.4 | モノレポビルドツール |

### Frontend (apps/*)

| カテゴリ | 技術 | バージョン | 用途 |
|---------|------|-----------|------|
| **Core** | React | ^19.2.3 | UIフレームワーク |
| **Core** | TypeScript | ~5.9.3 | 型安全性（strict mode） |
| **Core** | Vite | ^7.3.0 | ビルドツール |
| **UI** | Tailwind CSS | ^4.1.18 | スタイリング（CSS-first config） |
| **UI** | lucide-react | ^0.561.0 | アイコンライブラリ |
| **UI** | next-themes | ^0.4.6 | テーマ切り替え |
| **UI** | tailwindcss-animate | ^1.0.7 | アニメーション |
| **Data** | @tanstack/react-query | ^5.90.12 | データフェッチング・キャッシュ |
| **Data** | @tanstack/react-table | ^8.21.3 | テーブル管理 |
| **State** | Zustand | ^5.0.9 | クライアント状態管理 |
| **Form** | React Hook Form | ^7.68.0 | フォーム管理 |
| **Schema** | Zod | ^4.2.1 | スキーマバリデーション |
| **Admin** | react-admin | ^5.13.4 | 管理画面フレームワーク |
| **Quality** | Biome | ^2.3.9 | Lint + Format |
| **Test** | Vitest | ^4.0.16 | テスト |

### Backend (apps/web/server/)

| カテゴリ | 技術 | バージョン | 用途 |
|---------|------|-----------|------|
| **Core** | Hono | ^4.11.1 | 軽量・高速Webフレームワーク |
| **Server** | @hono/node-server | ^1.19.7 | Node.jsサーバーアダプター |
| **Schema** | @hono/zod-openapi | ^1.1.5 | Zodスキーマ + OpenAPI統合 |
| **Docs** | @scalar/hono-api-reference | ^0.9.28 | APIドキュメント UI |
| **RPC** | hono/client | - | 型安全なクライアント通信 |
| **Runtime** | Node.js | >=20.0.0 | 実行環境（tsx使用） |

### AI (packages/ai)

| カテゴリ | 技術 | バージョン | 用途 |
|---------|------|-----------|------|
| **Core** | ai | beta | Vercel AI SDK 6 コア |
| **Provider** | @ai-sdk/openai | beta | OpenAI プロバイダー |
| **React** | @ai-sdk/react | beta | React フック（useChat等） |

> 📝 AI SDK 環境変数の詳細は `.cursor/rules/ai-environment.mdc` を参照

---

## 🔧 Import Rules（モノレポ）

```typescript
// ✅ 共有パッケージからインポート
import { Button, Input, cn, toast } from '@myorg/ui';
import { UserSchema, type User } from '@myorg/shared';
import { openai, generateText, chatAgent } from '@myorg/ai';

// ✅ React用AIフック（各アプリで @ai-sdk/react を直接使用）
import { useChat } from '@ai-sdk/react';

// ✅ アプリ内パスエイリアス（@/）
import { useTodoStore } from '@/stores';
import { Tetris } from '@/components/admin/Tetris';

// ❌ 相対パスでの共有パッケージ参照は禁止
import { Button } from '../../packages/ui/src/Button';
```

### workspace:* 依存関係

```json
// apps/*/package.json
{
  "dependencies": {
    "@myorg/ai": "workspace:*",
    "@myorg/shared": "workspace:*",
    "@myorg/ui": "workspace:*",
    "@ai-sdk/react": "beta"
  }
}
```

---

## 🚫 FORBIDDEN - 絶対禁止

### any型の使用禁止

```typescript
// ❌ BAD
const data: any = fetchData();

// ✅ GOOD
const data = UserSchema.parse(await fetchData());
```

### 直接的な型定義（Zodスキーマなし）禁止

```typescript
// ❌ BAD
interface User {
  id: string;
  name: string;
}

// ✅ GOOD - packages/shared で定義
const UserSchema = z.object({
  id: z.string(),
  name: z.string(),
});
type User = z.infer<typeof UserSchema>;
```

### let宣言禁止（参照透過性違反）

```typescript
// ❌ BAD
let count = 0;
count++;

// ✅ GOOD
const newCount = count + 1;
const newItems = [...items, newItem];
```

### UIコンポーネントの重複定義禁止

```typescript
// ❌ BAD - apps内でUIコンポーネントを再定義
// apps/web/src/components/ui/Button.tsx

// ✅ GOOD - packages/uiから使用
import { Button } from '@myorg/ui';
```

---

## ✅ REQUIRED - 必須パターン

### Pattern 1: Zodスキーマファースト（packages/shared）

```typescript
// packages/shared/src/schemas.ts
import { z } from 'zod';

export const UserSchema = z.object({
  id: z.string().uuid(),
  name: z.string().min(1).max(100),
  email: z.string().email(),
  status: z.enum(['active', 'inactive', 'pending']),
  createdAt: z.coerce.date(),
});

export type User = z.infer<typeof UserSchema>;

// 作成用スキーマ
export const CreateUserSchema = UserSchema.omit({ 
  id: true, 
  createdAt: true 
});
export type CreateUser = z.infer<typeof CreateUserSchema>;
```

### Pattern 2: 共有UIコンポーネント（packages/ui）

> 📦 **主要依存関係**: `class-variance-authority` (CVA), `clsx` + `tailwind-merge`, `sonner`, `lucide-react`, `@radix-ui/*`

```typescript
// packages/ui/src/Button.tsx
import { cn } from './utils.js';
import { cva, type VariantProps } from 'class-variance-authority';

const buttonVariants = cva('...base styles...', {
  variants: { variant: { default: '...', destructive: '...' } },
});

export const Button: FC<ButtonProps & VariantProps<typeof buttonVariants>> = (
  { className, variant, ...props }
) => <button className={cn(buttonVariants({ variant }), className)} {...props} />;

// packages/ui/src/index.ts
export { AlertDialog, AlertDialogAction, ... } from './AlertDialog.js';
export { Button, buttonVariants } from './Button.js';
export { Checkbox } from './Checkbox.js';
export { Input } from './Input.js';
export { Toaster, toast } from './Toaster.js';
export { cn } from './utils.js';
```

### Pattern 3: アプリからの共有パッケージ利用

```typescript
// apps/web/src/components/admin/TodoList.tsx
import { Button, Input, AlertDialog, cn, toast } from '@myorg/ui';
import type { User } from '@myorg/shared';
```

---

## 📁 Naming Conventions

| 種類 | 規則 | 例 |
|------|------|-----|
| **パッケージ名** | `@myorg/*` | `@myorg/web`, `@myorg/ui` |
| **コンポーネント** | `PascalCase.tsx` | `UserList.tsx` |
| **ユーティリティ** | `camelCase.ts` | `formatDate.ts` |
| **ストア** | `camelCaseStore.ts` | `userStore.ts` |
| **スキーマ** | `schemas.ts` | `packages/shared/src/schemas.ts` |
| **テスト** | `*.test.ts` / `*.test.tsx` | `UserList.test.tsx` |
| **ファイル名** | 最大4単語連結 | `userListTable.tsx` |
| **Gitブランチ** | 最大4単語連結 | `feature/user-list-table` |

---

## ✅ Quality Gates

コミット前に必ず実行（Turborepoで並列実行）：

```bash
pnpm typecheck && pnpm lint && pnpm test && pnpm build
```

**全てパスしない限りコミット禁止** 🚫

---

## 🚦 Endpoints

### 開発環境

| URL | アプリ | 説明 |
|-----|--------|------|
| `http://localhost:5173` | @myorg/web | メインWebアプリ |
| `http://localhost:5174` | @myorg/admin | 管理画面 |
| `http://localhost:5175` | @myorg/playground | 実験場（ライフゲームなど） |
| `http://localhost:3000` | API (web) | バックエンドAPIサーバー |
| `http://localhost:3000/api/ui` | API (web) | Swagger UI |
| `http://localhost:3000/api/doc` | API (web) | OpenAPI JSON |

### Vercel環境（マルチプロジェクト）

| プロジェクト | Root Directory | URL |
|-------------|----------------|-----|
| myorg-web | `apps/web` | `https://myorg-web.vercel.app` |
| myorg-admin | `apps/admin` | `https://myorg-admin.vercel.app` |
| myorg-playground | `apps/playground` | `https://myorg-playground.vercel.app` |

> ⚠️ **Edge Runtime 制約**: Vercel Edge Runtime では `@myorg/shared`、`@hono/zod-openapi`、`@scalar/hono-api-reference` などのモジュールがサポートされません。そのため：
> - `api/[[...route]].ts`: シンプルなインラインHonoアプリを使用
> - `middleware.ts`: 純粋なJavaScriptで Basic認証を実装
> - ローカル開発では `server/app.ts` の完全版APIを使用

---

## 🔧 Configuration Files

| ファイル | 場所 | 用途 |
|---------|------|------|
| `pnpm-workspace.yaml` | ルート | ワークスペース定義 |
| `turbo.json` | ルート | Turborepoタスク定義 |
| `package.json` | ルート | 共通devDeps + turboスクリプト |
| `biome.json` | ルート | 共通Lint/Format設定 |
| `tsconfig.json` | ルート | TSプロジェクトreferences |
| `vite.config.ts` | 各apps/* | Viteビルド設定 |
| `vercel.json` | 各apps/* | Vercelデプロイ設定 |

---

## 🆕 新しいアプリの追加方法

> 📝 詳細な手順は `.cursor/commands/setup.md` を参照

### 必要なファイル一覧

| ファイル | 用途 |
|---------|------|
| `package.json` | パッケージ定義（`@myorg/shared`, `@myorg/ui` を依存に追加） |
| `tsconfig.json` | TypeScript設定（パスエイリアス `@/*` 含む） |
| `vite.config.ts` | Viteビルド設定（ポート番号の重複に注意） |
| `index.html` | HTMLエントリーポイント |
| `src/index.css` | Tailwind CSS v4 設定（CSS-first config） |
| `src/main.tsx` | Reactエントリーポイント |
| `src/App.tsx` | ルートコンポーネント |

### ポート番号の割り当て

| アプリ | Viteポート | APIポート |
|--------|------------|-----------|
| @myorg/web | 5173 | 3000 |
| @myorg/admin | 5174 | 3001 |
| @myorg/playground | 5175 | 3002 |
| 新アプリ | 5176〜 | 3003〜 |

### クイックスタート

```bash
# 1. ディレクトリ作成
mkdir -p apps/新アプリ/src

# 2. 各ファイルを作成（setup.md参照）

# 3. 依存関係インストール
pnpm install

# 4. 開発サーバー起動
pnpm --filter @myorg/新アプリ dev
```

### 完了チェックリスト

- [ ] `http://localhost:ポート番号` でアプリが表示される
- [ ] `@myorg/ui` のコンポーネントが動作する
- [ ] `pnpm typecheck` が通る
- [ ] `pnpm lint` が通る

---

## 🔐 Environment Variables

### 必須環境変数

| 項目 | 環境変数 | 説明 |
|------|---------|------|
| **OpenAI APIキー** | `OPENAI_API_KEY` | AI機能に必須 |
| **Basic認証ユーザー** | `BASIC_AUTH_USERNAME` | API認証（デフォルト: admin） |
| **Basic認証パスワード** | `BASIC_AUTH_PASSWORD` | API認証（デフォルト: admin） |

> ⚠️ **セキュリティ注意**: デフォルト値は開発用です。本番環境では `.env` で必ず変更してください。
> 📝 AI API キーの詳細は `.cursor/rules/ai-environment.mdc` を参照

### ローカル環境（.env）

```bash
# プロジェクトルートに .env ファイルを作成
OPENAI_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
BASIC_AUTH_USERNAME=admin
BASIC_AUTH_PASSWORD=admin
```

### Vercel環境変数の設定

```bash
# 対話式で設定
vercel env add OPENAI_API_KEY production
vercel env add BASIC_AUTH_USERNAME production

# .envファイルから一括設定
while IFS='=' read -r key value; do
  [[ -z "$key" || "$key" =~ ^# ]] && continue
  echo "$value" | vercel env add "$key" production --yes
done < .env.production

# Vercelからローカルへ取得
vercel env pull .env.local
```

> 📝 詳細は `.cursor/commands/setup.md` の「環境変数の一括設定」を参照
